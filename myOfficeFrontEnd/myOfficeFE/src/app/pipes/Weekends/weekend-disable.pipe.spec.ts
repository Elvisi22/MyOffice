import { WeekendDisablePipe } from './weekend-disable.pipe';

describe('WeekendDisablePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekendDisablePipe();
    expect(pipe).toBeTruthy();
  });
});
